[project]
name = "server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "plaid-python>=43.0.0",
]
[tool.pyright]
venvPath = "."
venv = ".venv"
